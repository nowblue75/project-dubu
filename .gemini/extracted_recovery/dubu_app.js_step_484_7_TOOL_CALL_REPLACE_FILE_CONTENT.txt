{
  "type": "TOOL_CALL_REPLACE_FILE_CONTENT",
  "stepIndex": 484,
  "createdAt": "2026-05-30T23:20:38Z",
  "targetFile": "\"C:/Users/USER/OneDrive/Desktop/컨텐츠작업/프로젝트 두부/dubu_app.js\"",
  "content": "\"            // 타이틀 길이별 폰트 스타일 지정 및 긴 타이틀 두 줄 처리\\n            const titleLength = p.title.length;\\n            let titleStyle = '';\\n            let displayTitle = p.title;\\n\\n            if (titleLength <= 5) {\\n                titleStyle = 'font-size: 0.95rem; font-weight: 700; letter-spacing: 2.2px;';\\n            } else if (titleLength <= 9) {\\n                titleStyle = 'font-size: 0.86rem; font-weight: 700; letter-spacing: 1.6px;';\\n            } else {\\n                // 10자 이상인 경우 두 줄 세로 배치 가공\\n                const mid = Math.floor(titleLength / 2);\\n                let bestSpaceIdx = -1;\\n                let minDiff = Infinity;\\n                \\n                for (let idx = 0; idx < p.title.length; idx++) {\\n                    if (p.title[idx] === ' ') {\\n                        const diff = Math.abs(idx - mid);\\n                        if (diff < minDiff) {\\n                            minDiff = diff;\\n                            bestSpaceIdx = idx;\\n                        }\\n                    }\\n                }\\n                \\n                if (bestSpaceIdx !== -1 && minDiff <= 3) {\\n                    displayTitle = p.title.substring(0, bestSpaceIdx) + '<br>' + p.title.substring(bestSpaceIdx + 1);\\n                } else {\\n                    displayTitle = p.title.substring(0, mid) + '<br>' + p.title.substring(mid);\\n                }\\n                titleStyle = 'font-size: 0.74rem; font-weight: 700; letter-spacing: 0.8px; line-height: 1.3; text-align: center;';\\n            }\\n\\n            const width = 78; // 고정된 넉넉한 책등 두께\\n            const height = (230 + Math.cos(seed * 37) * 8).toFixed(0); // 222px ~ 238px 높이의 자연스러운 입체 변차\\n\\n            const customStyles = `width: ${width}px; height: ${height}px;`;\\n\\n            booksHtml += `\\n                <div class=\\\"book-card\\\" id=\\\"book-card-${p.id}\\\" style=\\\"${customStyles}\\\" onclick=\\\"openMagicBook(event, ${p.id}\n<truncated 413 bytes>",
  "args": {
    "AllowMultiple": "false",
    "Description": "\"책장의 긴 책등 제목이 잘리지 않고 예쁜 비율의 두 줄 세로 쓰기로 표현되도록 렌더링 로직을 개선했습니다.\"",
    "EndLine": "1012",
    "Instruction": "\"긴 책등 제목 두 줄 가공 렌더링 반영\"",
    "ReplacementContent": "\"            // 타이틀 길이별 폰트 스타일 지정 및 긴 타이틀 두 줄 처리\\n            const titleLength = p.title.length;\\n            let titleStyle = '';\\n            let displayTitle = p.title;\\n\\n            if (titleLength <= 5) {\\n                titleStyle = 'font-size: 0.95rem; font-weight: 700; letter-spacing: 2.2px;';\\n            } else if (titleLength <= 9) {\\n                titleStyle = 'font-size: 0.86rem; font-weight: 700; letter-spacing: 1.6px;';\\n            } else {\\n                // 10자 이상인 경우 두 줄 세로 배치 가공\\n                const mid = Math.floor(titleLength / 2);\\n                let bestSpaceIdx = -1;\\n                let minDiff = Infinity;\\n                \\n                for (let idx = 0; idx < p.title.length; idx++) {\\n                    if (p.title[idx] === ' ') {\\n                        const diff = Math.abs(idx - mid);\\n                        if (diff < minDiff) {\\n                            minDiff = diff;\\n                            bestSpaceIdx = idx;\\n                        }\\n                    }\\n                }\\n                \\n                if (bestSpaceIdx !== -1 && minDiff <= 3) {\\n                    displayTitle = p.title.substring(0, bestSpaceIdx) + '<br>' + p.title.substring(bestSpaceIdx + 1);\\n                } else {\\n                    displayTitle = p.title.substring(0, mid) + '<br>' + p.title.substring(mid);\\n                }\\n                titleStyle = 'font-size: 0.74rem; font-weight: 700; letter-spacing: 0.8px; line-height: 1.3; text-align: center;';\\n            }\\n\\n            const width = 78; // 고정된 넉넉한 책등 두께\\n            const height = (230 + Math.cos(seed * 37) * 8).toFixed(0); // 222px ~ 238px 높이의 자연스러운 입체 변차\\n\\n            const customStyles = `width: ${width}px; height: ${height}px;`;\\n\\n            booksHtml += `\\n                <div class=\\\"book-card\\\" id=\\\"book-card-${p.id}\\\" style=\\\"${customStyles}\\\" onclick=\\\"openMagicBook(event, ${p.id}\n<truncated 413 bytes>",
    "StartLine": "986",
    "TargetContent": "\"            // 타이틀 길이별 폰트 스타일 지정 (가독성 상향 조정)\\n            const titleLength = p.title.length;\\n            let titleStyle = '';\\n            if (titleLength <= 5) {\\n                titleStyle = 'font-size: 0.95rem; font-weight: 700; letter-spacing: 2.2px;';\\n            } else if (titleLength <= 8) {\\n                titleStyle = 'font-size: 0.86rem; font-weight: 700; letter-spacing: 1.6px;';\\n            } else if (titleLength <= 11) {\\n                titleStyle = 'font-size: 0.78rem; font-weight: 700; letter-spacing: 1.0px; line-height: 1.25;';\\n            } else {\\n                titleStyle = 'font-size: 0.70rem; font-weight: 700; letter-spacing: 0.5px; line-height: 1.15;';\\n            }\\n\\n            const width = 78; // 고정된 넉넉한 책등 두께\\n            const height = (230 + Math.cos(seed * 37) * 8).toFixed(0); // 222px ~ 238px 높이의 자연스러운 입체 변차\\n\\n            const customStyles = `width: ${width}px; height: ${height}px;`;\\n\\n            booksHtml += `\\n                <div class=\\\"book-card\\\" id=\\\"book-card-${p.id}\\\" style=\\\"${customStyles}\\\" onclick=\\\"openMagicBook(event, ${p.id})\\\">\\n                    <div class=\\\"book-spine ${themeClass}\\\">\\n                        <div class=\\\"spine-vol\\\">${magicSymbol} VOL.${p.id}</div>\\n                        <div class=\\\"spine-title\\\" style=\\\"${titleStyle}\\\">${p.title}</div>\\n                        <div class=\\\"spine-icon\\\"><i class=\\\"fa-solid ${iconClass}\\\"></i></div>\\n                    </div>\\n                </div>\\n            `;\"",
    "TargetFile": "\"C:/Users/USER/OneDrive/Desktop/컨텐츠작업/프로젝트 두부/dubu_app.js\"",
    "toolAction": "\"dubu_app.js 파일 교체 수정\"",
    "toolSummary": "\"dubu_app.js 내의 책등 제목 지정 로직을 두 줄 처리 분할 알고리즘으로 교체합니다.\""
  }
}